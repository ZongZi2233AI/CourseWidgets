# 版本统一管理方案

## 📋 概述

本项目采用**集中式版本管理**，所有版本号都定义在 `lib/constants/version.dart` 文件中，通过自动化脚本同步到所有配置文件。

## 🎯 版本定义位置

### 主版本文件
**文件**: `lib/constants/version.dart`

```dart
/// 应用版本号
const String appVersion = '1.0.6';
const int buildNumber = 10000006;

/// 版本显示文本
const String versionText = 'v$appVersion ($buildNumber)';

/// 应用名称
const String appName = 'CourseWidgets';
```

### 自动同步的文件
1. **Android**: `android/app/build.gradle.kts`
2. **Windows**: `windows/runner/Runner.rc`
3. **Flutter**: `pubspec.yaml`
4. **UI界面**: 所有Dart文件通过import使用常量

## 🚀 使用方法

### 1. 修改版本号
只需修改 `lib/constants/version.dart` 中的版本常量：

```dart
const String appVersion = '1.0.7';  // 修改版本号
const int buildNumber = 10000007;   // 修改构建号
```

### 2. 同步版本号
运行同步脚本：

```bash
cd schedule_app
python sync_versions.py
```

### 3. 脚本自动完成
- ✅ 更新Android版本配置
- ✅ 更新Windows版本配置  
- ✅ 更新pubspec.yaml
- ✅ 所有UI界面自动生效

## 📁 版本影响范围

### Windows端
- 窗口标题: `CourseWidgets v1.0.6`
- 关于页面显示版本
- 文件属性中的版本信息
- 系统托盘提示

### Android端  
- APK版本号: versionCode=10000006
- APK版本名: versionName="1.0.6"
- 关于页面显示版本
- 应用信息显示

### macOS端
- 关于页面显示版本
- 应用菜单显示

### Web端
- 页面标题
- 关于页面显示版本

## 🔧 技术实现

### Dart常量导入
```dart
import '../../constants/version.dart';

// 使用常量
Text('$appName v$appVersion')
Text('Build $buildNumber | v$appVersion')
```

### Python同步脚本
- 读取Dart常量文件
- 解析版本号和构建号
- 正则替换各配置文件
- 保持版本一致性

## 📝 版本更新示例

### 从 v1.0.6 更新到 v1.0.7

1. **修改常量**:
   ```dart
   // lib/constants/version.dart
   const String appVersion = '1.0.7';
   const int buildNumber = 10000007;
   ```

2. **运行同步**:
   ```bash
   python sync_versions.py
   ```

3. **结果**:
   - ✅ Android: versionCode=10000007, versionName="1.0.7"
   - ✅ Windows: VERSION_AS_NUMBER=1,0,7,0, VERSION_AS_STRING="1.0.7"
   - ✅ Flutter: version: 1.0.7+10000007
   - ✅ 所有UI界面自动更新

## ✅ 优势

1. **单一真实来源**: 只有一个地方需要修改版本号
2. **自动化同步**: 无需手动修改多个文件
3. **减少错误**: 避免版本号不一致
4. **易于维护**: 版本管理逻辑集中
5. **可扩展**: 可以轻松添加更多配置文件

## 🛠️ 扩展建议

### 添加新平台
如果需要支持新平台，只需在同步脚本中添加对应的更新函数：

```python
def update_new_platform(version, build_number):
    new_file = "path/to/config"
    # 读取文件
    # 替换版本号
    # 保存文件
```

### 添加更多常量
可以在 `version.dart` 中添加更多应用常量：

```dart
const String appDescription = '应用描述';
const String appAuthor = '作者名称';
const String appWebsite = '网站地址';
```

## 📊 当前版本状态

- **当前版本**: v1.0.6 (10000006)
- **最后同步**: 2025-12-30 10:13:00
- **同步状态**: ✅ 已同步
- **所有平台**: ✅ 版本一致

---

**版本管理由 `sync_versions.py` 自动化维护**
*最后更新: 2025-12-30*
